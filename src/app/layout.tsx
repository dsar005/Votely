import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Votely – Your Personal Election Assistant",
  description:
    "An AI-powered assistant that helps you understand voting eligibility, explore your ballot, practice voting, and prepare for election day in just 3 minutes.",
  keywords: ["voting", "election", "ballot", "eligibility", "voter guide"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="font-sans antialiased">
        {children}
      </body>
    </html>
  );
}
